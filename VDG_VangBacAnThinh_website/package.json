{
  "name": "vdg-vangbacanthinh-website",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "admin"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:admin\"",
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run develop --workspace=backend",
    "dev:admin": "npm run dev --workspace=admin",
    "build": "npm run build --workspace=frontend && npm run build --workspace=backend && npm run build --workspace=admin",
    "build:frontend": "npm run build --workspace=frontend",
    "build:backend": "npm run build --workspace=backend",
    "build:admin": "npm run build --workspace=admin"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
